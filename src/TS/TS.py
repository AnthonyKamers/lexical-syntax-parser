'''
[] - Criar tabela(dict) de simbolos apartir das palavras reservadas.
    
[] - Percorrer o texto para pegar o proximo lexema, separando por espaços.

[] - Verifica se o lexema encontrado ja esta na TS, caso esteja, retorna seu identify_tokens, caso nao (verifica se é uma variael nova? )..?

        Tem q usar o padrao <nome-token, valor-atributo> ? 
        Onde nome-token é o identificador dele na TS e valor-atributo é a posição na TS, 
        porem < + > é um lexema para token, que nao precisa de valor. 


[] - Tem que criar uma tabela com todos os simbolos aceitos pela gramatica, (a-z, A-Z, 0-9, ...). Criado nessa tarefa?

'''

# seria uma classe para a TS? 

# tabela_simbolos = dict()
# tokens = list()
# alfabeto = dict()


def creat_TS():
    pass

def parse_line():
    pass

def get_token():
    pass
